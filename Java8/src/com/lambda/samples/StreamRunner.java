package com.lambda.samples;

import java.util.List;
import java.util.stream.Collectors;

public class StreamRunner {
	public static void main(String[] args) {
		Employee caller = new Employee();
		System.out.println(caller.getName().indexOf(ch));
		 //List<Long> salaryList = caller.empList.stream().filter(e->e.getSalary()>50000)
				 						//.map(m->m.getSalary())
				 						//.collect(Collectors.toList());
		 //salaryList.forEach(a->System.out.println(a));
	}
}
